<form method="post" action="{{ path('simple_appointment_delete', {'id': simple_appointment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ simple_appointment.id) }}">
    <button class="btn">Delete</button>
</form>
