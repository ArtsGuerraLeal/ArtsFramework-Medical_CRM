{% extends 'base.html.twig' %}

{% block title %}SimpleAppointment{% endblock %}

{% block body %}
    <h1>SimpleAppointment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ simple_appointment.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ simple_appointment.date ? simple_appointment.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('simple_appointment_index') }}">back to list</a>

    <a href="{{ path('simple_appointment_edit', {'id': simple_appointment.id}) }}">edit</a>

    {{ include('simple_appointment/_delete_form.html.twig') }}
{% endblock %}
