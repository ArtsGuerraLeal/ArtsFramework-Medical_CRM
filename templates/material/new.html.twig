{% extends 'base_admin.html.twig' %}

{% block title %}New Material{% endblock %}

{% block body %}

<div class="card">
    <div class="card-header">Material
    </div>
    
        <div class="card-body">
 
        {{ include('material/_form.html.twig', {'button_label': 'Guardar'}) }}

        </div>

        
</div>
    <a href="{{ path('material_index') }}">back to list</a>
{% endblock %}
