{% extends 'base_admin.html.twig' %}

{% block title %}Vendor{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header"><i class=""></i>Proveedor
    </div>
    <div class="card-body">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vendor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vendor.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ vendor.email }}</td>
            </tr>
        </tbody>
    </table>

  
    <div class="btn btn-primary btn-sm my-1">
        <a class="text-white" href="{{ path('vendor_index', {'id': vendor.id}) }}">Detalles</a>
    </div>
    <div class="btn btn-primary btn-sm">
        <a class="text-white" href="{{ path('vendor_edit', {'id': vendor.id}) }}">Editar</a>
    </div>
    </div>

{% endblock %}
