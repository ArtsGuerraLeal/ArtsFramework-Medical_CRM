<form method="post" action="{{ path('ensamble_delete', {'id': ensamble.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ensamble.id) }}">
    <button class="btn">Delete</button>
</form>
