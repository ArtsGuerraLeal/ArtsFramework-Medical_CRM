{% extends 'base_admin.html.twig' %}

{% block title %}Area{% endblock %}

{% block body %}
    <h1>Area</h1>
    <div class="card">
    <div class="card-header"><i class="fas fa-table"></i>Areas</div>
        <div class="card-body">
 <table class="table table-bordered table-striped " id="zero_config" >
         <tbody>
            <tr>
                <th>Id</th>
                <td>{{ area.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ area.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('area_index') }}">back to list</a>

    <a href="{{ path('area_edit', {'id': area.id}) }}">edit</a>
</div>
</div>
    {{ include('area/_delete_form.html.twig') }}
{% endblock %}
